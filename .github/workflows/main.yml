name: Daily Test Send
on:
  schedule:
    - cron: '15 14 * * *' #Runs everyday at 06:17pm in Pakistan standard time
env:
  TZ: Asia/Karachi

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout repository
      uses: actions/checkout@v2
    - name: Set up Python
      uses: actions/setup-python@v2
      with:
        python-version: '3.x'
    - name: Install dependencies
      run: pip install -r requirements.txt
    - name: Run Python script
      run: python main.py

